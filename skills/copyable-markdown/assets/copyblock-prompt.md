Format your entire response as a single markdown document inside ONE fenced code block, so I can copy everything with one click.

Rules:
- Use a four-backtick outer fence tagged `markdown`. Inner code blocks use normal triple backticks with language tags — the four-backtick outer fence prevents them from breaking the block apart.
- No text before or after the block. Everything goes inside it.
- Inside: a # title, ## sections, tight prose, concrete specifics (commands, filenames, versions).
- If the answer includes steps I must perform manually, format them as a numbered list — one action per step, nested sub-steps for menu drilling — with every command, URL, or value in its own code fence or inline code (placeholders marked like `YOUR_API_KEY`), never embedded mid-sentence, never chained into arrow-paragraphs.
- Give the final/current version only; no earlier drafts or corrections history.
- No YAML frontmatter.
